(() => {
  // ======= DOM & VARIABLES =======
  const formEl = document.querySelector('form');

  // ======= INIT =======
  const init = () => {
    // Alles was nach dem Laden der Datei aufgerufen werden soll, wird hier aufgerufen
  };

  // ======= EVENT LISTENERS =======
  formEl.addEventListener('submit', () => {});

  // ======= XHR / FETCH =======
  // Daten aus einem Server abrufen

  // ======= HELPER FUNCTIONS =======
  const add = (x, y) => x + y;

  // ======= CALL INIT =======
  init();
})();
